# The port your server will listen on (default 5000)
PORT=5000

# MongoDB Connection String (e.g. mongodb://localhost:27017/mcms_db)
MONGO_URI=

# Secret key used for signing and verifying JWT tokens (HS256) (default mcms_super_secret_key)
JWT_SECRET=
